# TODO

*Open threads from a previous session. Treat as starting context, not instructions — verify relevance before acting.*

## Known limitations

- **Saturation incompleteness**: Some derivable theorems (double complement, zero annihilation, additive commutativity in rings) require reverse identity/absorption rules that cause e-graph blowup. These are added as explicit axioms instead. A more principled approach to controlled term introduction would help.
- **No binder support**: Can't express quantified axioms or lambda terms in the s-expression language.
- **Flat s-expressions**: Pattern matching in `classify.rs` is fragile — adding new axiom shapes requires new detector functions.
- **Discover: shared e-graph saturation blowup**: Morphism discovery builds a separate e-graph per (axiom, candidate-combo) check. A single shared e-graph would be faster (one saturation pass), but theories with distributivity/commutativity (e.g. rings) blow up when many candidate-translated expressions are added together. Incremental saturation or e-graph cloning could fix this.
- **Discover: enumerate-then-check**: Template candidates are generated blind to axiom structure, then brute-force checked. When axioms tightly constrain the morphism (e.g. `sub(a,a) = zero`, `sub(a,zero) = a` together force `sub → add + negate`), a synthesis approach that derives the template from axiom constraints would avoid enumeration entirely.
- **Discover: argument dropping**: `uses_all_args` rejects templates that don't reference every positional arg, ruling out valid interpretations like projections (`proj(a,b) → a`) and constant maps (`absorb(a) → zero`).
- **Discover: depth ceiling**: Template depth is bounded by enumeration. Interesting morphisms can require deep templates (depth 4+) where candidate counts are already intractable at depth 2 against larger signatures.
- **One-way morphisms only**: `discover` finds source→target interpretations. Morita equivalence — two theories that interpret each other but share no direct signature morphism (e.g. groups via `{e, inv, mul}` vs `{div}`) — requires round-trip discovery and equivalence-of-interpretations checking, neither of which exists yet.

## Vision: proof comprehension

Long-term goal: given any proof, explain every step by finding its structural meaning — what move is being made, and where that same move appears elsewhere in mathematics. The "graph of all proofs" falls out naturally as exhaust from this process.

Key ideas:
- A proof step is represented in *all* extractable projections simultaneously (formal term, equational content, categorical structure, natural language, etc.) — all added to the e-graph as equivalent nodes
- The architecture is intentionally substrate-neutral: nodes carry arbitrary content, edges are typed relations ("is a projection of," "is a proof of," "is an instance of"), e-graph handles equivalence
- Content (which nodes matter, which rewrites are meaningful) is the research problem; the substrate shouldn't foreclose anything

Near-term: **Lean/Mathlib proof step extraction and annotation**
1. Pick a simple Mathlib proof (basic group theory result)
2. Extract proof steps (tactic tree or term)
3. Extracted data (tactic tree, premises, statement metadata) is itself a
   valid native representation — not raw material to translate into motif's
   `Signature`/`Theory` vocabulary before it "counts." (Corrected 2026-07-25,
   user direction — see `docs/research/vision.md` principle 9: motif has no
   single canonical substrate everything funnels through.)
4. Once enough corpus data is extracted, look for structural relationships
   across it directly in its native form: symmetries, isomorphisms,
   translations between pieces/regions, and gaps/density variation in what's
   proven vs. what neighboring structure would suggest. Kept intentionally
   vague — this is a near-term *direction*, not a designed plan; concrete
   cross-representation relationship-finding machinery is future work.
5. Output: annotated proof/corpus region where structural relationships found
   so far are recorded

This is the smallest concrete exercise of the full vision, and the gaps it reveals drive what to build next.

**2026-07-25 extraction scaffolding**: two working extraction paths, both run successfully against Mathlib built inside `~/git/lean-pool`.

1. **Statement-level extraction** — lean-pool's own `scripts/exposition/Extract.lean` (external repo, not vendored here), run via `lake env lean --run` after retargeting its `poolRoot` constant at an arbitrary Mathlib module (e.g. `Mathlib.Algebra.Group.Defs`). Schema at `~/git/lean-pool/python/lean_pool/exposition/SCHEMA.md`; one JSON object per declaration:
   ```json
   {"id": "Foo.aux", "n": "Foo.aux", "m": "Mathlib.Algebra.Group.Defs",
    "k": "theorem", "r": [435, 0, 446, 23], "s": [435, 6],
    "deps": ["<id>", ...], "ext": 127}
   ```
   Extracted 356 declarations from one file in seconds (after `lake update mathlib` cache-built, ~2m48s, cache hit, no source build).

2. **Proof-structure extraction** — `langlands/scripts/lean-extraction/ExtractOne.lean`, ported from LeanDojo v1's `ExtractData.lean` (LeanDojo's Python package is deprecated; this Lean-side logic walks Lean's stable `Elab.InfoTree` API directly, no LeanDojo dependency). Per declaration: full tactic tree with before/after pretty-printed proof-state text at each step (including sub-goal branches, e.g. induction cases), plus a premise trace (which prior lemmas/constants each tactic step invokes, with source positions). Invocation:
   ```
   lake env lean --run langlands/scripts/lean-extraction/ExtractOne.lean <mathlib-file> <startLine> <endLine>
   ```
   run from inside `~/git/lean-pool` (so `LEAN_PATH` resolves its built Mathlib oleans), under `nix develop langlands --command ...` (langlands' flake provisions elan; lean-pool's `lean-toolchain` pins v4.32.0-rc1, distinct from langlands' own v4.32.1 — elan switches per-project toolchain automatically). Verified on `mul_pow_mul` in `Mathlib.Algebra.Group.Defs` (real multi-step induction proof, `zero`/`succ` case branching, `succ` case shows premises `pow_succ'`/`mul_assoc`); re-verified after moving the script into this repo. Example output shape:
   ```json
   {"tactics": [{"stateBefore": "case zero\n...\n⊢ (a * b) ^ 0 * a = a * (b * a) ^ 0",
                 "stateAfter": "no goals", "pos": 29161, "endPos": 29165}],
    "premises": [{"fullName": "mul_pow_mul", "modName": "...Defs",
                  "pos": {"line": 693, "column": 21}, "endPos": {"line": 693, "column": 32}}]}
   ```
   Porting note (only break going from LeanDojo's Lean v4.20 pin to v4.32.0-rc1): `String.Pos` became an indexed structure (`structure String.Pos (s : String)`, part of a byte/codepoint safety overhaul); fixed by using `String.Pos.Raw` (the plain `{byteIdx : Nat}` struct `Syntax.getPos?`/`FileMap` APIs already return) and projecting `.byteIdx` in the `ToJson` instance. Everything else (`InfoTree`, `TacticInfo`, `ContextInfo.runMetaM`, `Command.mkState`, `IO.processCommands`) ported unchanged.

   Considered ntp-toolkit (CMU L3 lab) as an alternative — actively maintained, lighter footprint, but flatter output schema (flat `(state, nextTactic)` JSONL rows, no explicit tactic tree/premises) — chose the LeanDojo InfoTree-porting approach for full fidelity instead. LeanDojo-v2 (actively maintained successor to the deprecated v1 package) not evaluated in depth — unexplored.

   Corpus scope (decided): mathlib4 (full, ~300K declarations order of magnitude) + lean-pool's own indexed projects + TauCeti (no extraction tooling of its own yet — needs the same approach pointed at it). Extract structure of both proofs and proven statements, kept in their native Lean-derived form (tactic tree, premises, statement metadata) — no requirement to flatten into motif's quantifier-free `Signature` representation first (see "No single substrate" correction above; the typeclass-hierarchy/binder-heavy-statement vs. flat `Signature` gap noted in "No binder support" above is a real gap for *that one representation*, not a blocker for corpus analysis in general).

   Next steps (unstarted):
   - Run `ExtractOne.lean` across a small batch of files to check the porting note generalizes (only tested on one declaration so far).
   - Once there's enough extracted corpus data, start looking for structural relationships across it in its native form (step 4 of the near-term plan above) — direction only, no design yet.

**2026-07-25 structure-discovery prototypes**: three throwaway probes built against the 360-declaration `group-cross-file-sample.jsonl` corpus to test different notions of structure discovery — see `langlands/scripts/lean-extraction/README.md` ("Structure-discovery prototypes" section) for full results. Summary: dependency-set Jaccard clustering (`crates/motif-corpus/examples/paradigmatic_clusters.rs`) and cross-declaration similarity ranking (`morphism_candidates.rs`) both produced real signal and are being kept; string-encoded NCD/compression similarity (`compression_similarity.rs`) is paused — its headline `to_additive`-mirror result turned out circular (recovering a known mechanical transformation) and its whole approach has a deeper flaw (linearizing sets/graphs into strings smuggles in an arbitrary order that then contaminates any distance metric on top).

Next steps (direction, not a plan):
- `paradigmatic_clusters.rs`: fix the greedy transitive-closure artifact (one 42-member catch-all cluster from naive union-find chaining, A~B~C merged despite A≁C) — needs connected-components-with-min-density or real community detection instead of pairwise-threshold transitive closure.
- `morphism_candidates.rs`: the test corpus (`Defs.lean` + `Commutator.lean`) is effectively one theory, so every top-ranked pair was an already-obvious same-theory naming-convention dual. The user's stated real goal is finding shared structure between *unrelated* parts of the corpus — testing that needs a corpus spanning genuinely distinct theories (e.g. group + ring, or group + lattice), not more files within group theory.

Relevant fields to eventually handle: Langlands program, HoTT/CoC, elliptic curves, harmonic analysis (Kakeya hierarchy), BB(5)-style exhaustive classification, Curry-Howard-Lambek correspondence.

## Potential next work

- Vector space theory (scalar field + module axioms — first parameterized theory)
- Morphisms as first-class objects (homomorphisms within a theory, not just translations between)
- Translation/morphism DSL files for file-based workflow
- Axiom-guided morphism synthesis (use axiom constraints to derive templates instead of enumerating candidates)
- Bi-interpretability / Morita equivalence detection between theories
- **Mathlib: Henselian valued fields — algebraic extensions are Henselian.** "Valuation rings of algebraic extensions of a Henselian field are Henselian" is confirmed missing from Mathlib (`Mathlib/RingTheory/Henselian.lean` has only bare definitions + two lemmas, no integral/algebraic-extension or Krasner's-lemma-style machinery). The `UnramifiedExtension.lean` sorry this would have unblocked was since closed via the alternative discriminant-unit/monogenic-order route (`MonogenicMaximalOrder.lean`), so this is no longer a local blocker — but the Mathlib gap itself is still real and standalone-PR-sized, independent of this repo's needs.
- **Mathlib upstreaming candidates.** Re-checked 2026-08-05 against current vendored Mathlib + loogle: all six remain genuinely missing upstream — (1) cyclic-subgroup-by-divisor classification and (2) ℤ-subgroup-by-index classification (ingredients like `card_powMonoidHom_ker/range` exist in `SpecificGroups/Cyclic.lean`, the packaged statement doesn't), (3) `ProfiniteGrp` universe-bridging via `ULift` (zero `ULift` mentions in `Mathlib/Topology/Algebra/Category/ProfiniteGrp/`), (4) algebraicity-of-completions-under-finite-extension (Mathlib only has the `NumberField`-specialized instance, no generic version), (5) a `RankOne` instance for bare adic completions (Mathlib only has `NumberField.instRankOneAdicCompletion`, fixed to `e = absNorm`), (6) the discriminant-unit/monogenic-order lemma (`conductor_eq_top_iff_adjoin_eq_top` exists as an ingredient, no combined theorem). All six still real upstream-PR candidates; no PRs attempted, this was read-only investigation.
- **`WeilGroup.lean` polish (adjacent, non-blocking):** continuity of `toArt` — **closed** 2026-08-05 (`feat(langlands): prove continuity of toArt`, commit `a605022`): via `continuous_of_continuousAt_one`, reduces to `ker(toArt) = I_K` being a nhd of 1, which holds since `I_K` is open. The topological splitting `W_K ≅ I_K ⋊ ℤ` remains open — investigated and blocked: Mathlib has zero topological-semidirect-product machinery (`SemidirectProduct` has no `TopologicalSpace`/continuity API anywhere in vendored Mathlib), so this needs `toArt` surjectivity (not yet in the file), an explicit splitting/section construction, and either a from-scratch topological-`SemidirectProduct` instance or an alternate formulation — real new content, no scaffold to lean on, not attempted rather than guessed at.
- **Phase 2 (local CFT / Artin map), per `langlands/ROADMAP.md`:** Phase 0 and Phase 1 are complete and all sorries in `langlands/Langlands/*.lean` are closed (re-verified 2026-08-05: `grep -rn sorry langlands/Langlands/` empty, full `lake build` green, 8670 jobs). Phase 2 needs Brauer group + Hilbert 90 as prerequisites — both already present in Mathlib (`Mathlib/Algebra/BrauerGroup/`, `Mathlib/RepresentationTheory/Homological/GroupCohomology/Hilbert90.lean`), so the *listed* prerequisites are satisfied; what's not started is the large reciprocity-map construction itself, which the roadmap itself sizes as comparable to Serre's *Local Fields* chapters VIII–XIII combined — not a small next step. Duplication check against kbuzzard/ClassFieldTheory **re-verified fresh 2026-08-05** (repo last pushed 2026-08-03, so actively live): grepped the full file tree (~100 `.lean` files) and GitHub code search for "Weil" — zero hits anywhere in code or blueprint TOC. That project builds local CFT entirely via Tate/group cohomology, Herbrand quotients, and fundamental classes (`ClassFieldTheory/Cohomology/*`, `ClassFieldTheory/LocalCFT/{Continuity,Teichmuller}.lean`), not the Weil group. **Construction-level duplication check still holds — no direct overlap.** New information beyond the prior check: kbuzzard/ClassFieldTheory is a well-resourced, actively-developed project (2025 Clay Summer School repo, 16 stars/26 forks, commits within the last 2 days) with local CFT itself substantially underway on its cohomological route (a reciprocity isomorphism is already in its blueprint, §2.12). This doesn't create construction duplication with the Weil-group route, but it's a strategic consideration worth surfacing before sinking a multi-session effort into Phase 2: a mature alternative local-CFT formalization may land in the Lean ecosystem via that route regardless. Whether to still pursue the Weil-group route, and how to scope its first concrete milestone (no small stepping-stone task is defined in the roadmap — the "first move" section jumps straight to building the full reciprocity map), is a scoping call worth making deliberately rather than starting blind.

**Update 2026-08-05 — Phase 2a scoped, attempted, not closed.** Scoped a genuinely smaller first cut into `langlands/ROADMAP.md` ("Phase 2a — Unramified norm-group surjectivity"): the unramified/"easy half" of local CFT (Serre Ch. V §2–3, `N_{L/K}(L^×) = ⟨π⟩^n·O_K^×` for unramified `L/K`), scoped down further to just the residue-field-level statement `𝓀[L]^× ↠ 𝓀[K]^×`. A research pass confirmed the compositional pieces exist (`localNormMap` in `Langlands/NormMap.lean`, `ValuationSubring.surjective_unitGroupToResidueFieldUnits`, `GaloisField.norm_surjective`), but an implementation attempt found the one non-compositional piece — whether `localNormMap` (built from `Algebra.norm`) reduces mod the maximal ideal to the residue-field norm — does not exist in Mathlib and needs real new infrastructure (two routes identified and detailed in ROADMAP.md: a determinant/basis-reduction route via `Module.Free`/`IsLocalRing.linearCombination_bijective_of_flat`, or a Galois/Frobenius route via `Algebra.norm_eq_prod_automorphisms`/`prod_galRestrict_eq_norm`, both needing instances not yet in this repo). No file was written and no `sorry` was left — the attempt stopped cleanly rather than half-implementing. Next session: pick one of the two routes in ROADMAP.md's Phase 2a section and build its missing instance(s) first, in isolation.

**Update 2026-08-05 (second pass) — Phase 2a residue-field half CLOSED; compatibility square still open.** New file `langlands/Langlands/ResidueFieldNorm.lean` (165 lines, zero `sorry`, `lake build Langlands.ResidueFieldNorm` green, commit `3249a37`). The unlock was `Valuation.HasExtension` (`Mathlib/RingTheory/Valuation/Extension.lean`): `NormMap.lean:424`'s `adicCompletionIntegers_comap_eq` is exactly its hypothesis once run through `Valuation.isEquiv_iff_val_le_one`, giving `hasExtension_valued_adicCompletion` (`ResidueFieldNorm.lean:72`), which in turn supplies `Algebra K₀ L₀`, `IsLocalHom (algebraMap K₀ L₀)`, and — the piece that was missing — `Algebra 𝓀[K] 𝓀[L]`. (`adicCompletionIntegers` is a non-reducible `def` for `Valued.v.valuationSubring`, so the instances are re-stated via `inferInstanceAs` at `:92, 100, 108`; `coe_algebraMap_adicCompletionIntegers` (`:118`) and `algebraMap_residueField_residue` (`:127`), both `rfl`, pin them to `adicCompletionComap`.) With that, `FiniteField.norm_surjective`/`FiniteField.unitsMap_norm_surjective` give `residueField_norm_surjective` (`:146`) and `residueField_units_norm_surjective` (`:157`), i.e. **`𝓀[L]^× ↠ 𝓀[K]^×`**, under a `[Finite 𝓀[L]]` hypothesis (`R`, `S` are arbitrary Dedekind domains here, so residue finiteness isn't derivable). **This is the abstract residue-field norm, not `localNormMap`'s reduction** — unramifiedness is neither used nor assumed; the file's docstrings say so explicitly. The compatibility square was investigated, not attempted in code: loogle against vendored Mathlib gives 0 hits for `Module.Finite, Valuation.integer` / `Module.Free, Valuation.integer` (route 1's basis must be built from scratch), `IsLocalRing.linearCombination_bijective_of_flat` **does not exist** (correction to the prior entry — the real tools, `Module.exists_basis_of_basis_baseChange` / `IsLocalRing.span_eq_top_of_tmul_eq_basis`, need `Module.FinitePresentation K₀ L₀` as *input*), and a third blocker not previously named: the reduction step needs `𝔪_K·L₀ = 𝔪_L`, i.e. unramifiedness, which has no formulation at all in `NormMap.lean`'s arbitrary-`e` setting and no Mathlib `Algebra.IsUnramified` for valuation-ring extensions. The `AdjoinRoot.powerBasisAux'` shortcut was checked and does not apply: `UnramifiedExtension.lean:715`'s existence theorem doesn't expose its internal `AdjoinRoot f ≃+* C`, and its setting requires `[IsAlgClosed L]` ambient with the extension given as `K⟮x⟯` — bridging to `w.adicCompletion L` would mean proving unramified-implies-monogenic in the completion setting, which is the missing content, not a route around it. Full detail in `langlands/ROADMAP.md`, Phase 2a, "Status 2026-08-05 (second attempt)".

**Update 2026-08-05 (third pass) — re-verification only, no code, one new route checked and closed.** Re-verified live files and vendored Mathlib match the second-pass description exactly: same Mathlib commit `520045a` (`langlands/flake.lock` unmoved since project-skeleton commit `6df4f72`, so zero drift, not a newer checkout), `ResidueFieldNorm.lean`/`NormMap.lean`/`UnramifiedExtension.lean` unchanged line counts, `lake build Langlands.ResidueFieldNorm` green (3412 jobs), `grep -rn sorry langlands/Langlands/` empty. Re-ran every second-pass loogle query fresh — all still 0 hits (`Algebra.norm (?A ⧸ ?I)`, `LinearMap.det, Ideal.Quotient.mk`, `IsLocalRing.residue, Algebra.norm`, `Algebra.norm, Ideal.Quotient.mk`, `Module.Finite, Valuation.integer`, `Module.Free, Valuation.integer`, `Module.Finite, ValuationSubring`, `Module.Free, ValuationSubring`, `IsDiscreteValuationRing, Module.Finite`, `Valuation.Integers, Module.Finite`), confirming no Mathlib movement on this gap since the second pass. Checked a third route not previously considered: `IsIntegralClosure.finite` (`Mathlib/RingTheory/DedekindDomain/IntegralClosure.lean`) as a shortcut to `Module.Finite (v.adicCompletionIntegers K) (w.adicCompletionIntegers L)`, bypassing route 1's `Module.Free`/PID-theory path — `v.adicCompletionIntegers K` already has `IsDiscreteValuationRing` hence the Noetherian+integrally-closed hypotheses on the base ring. **Closed as a route**: it additionally needs `[IsIntegralClosure (w.adicCompletionIntegers L) (v.adicCompletionIntegers K) (w.adicCompletion L)]` as an instance, i.e. that the top valuation subring genuinely *is* the integral closure of the bottom one — and no lemma in vendored Mathlib supplies this for general valuation-ring extensions (`ValuationRing, IsIntegralClosure` → 0 hits, `Valuation.integer, IsIntegralClosure` → 0 hits, `Valuation.HasExtension, IsIntegral` → 0 hits, `FiniteDimensional, ValuationSubring, IsIntegral` → 0 hits). The hard direction of that fact (elements of `L₀` are integral over `K₀`) genuinely needs completeness (uniqueness of valuation extension to a finite extension of a complete field) and is itself Serre-level content, not a corollary `IsIntegralClosure.finite` would hand back. Also checked whether Mathlib's separate non-archimedean-analysis norm-extension theory (`Mathlib/Analysis/Normed/Unbundled/SpectralNorm.lean`, `spectralNorm`, de Frutos-Fernández et al.) could supply this — it's disconnected from this repo's `Valued`/`adicCompletion` framework (`spectralNorm, integralClosure` → 0 hits; no lemma equates `spectralNorm` with a `Valuation`/`Valued.v` instance anywhere in vendored Mathlib); bridging the two formalizations would itself be new work. No code was written — every candidate stopped at a landscape check, so nothing was left half-implemented, and `sorry` count stayed at zero throughout. Status unchanged from second pass: residue-field half closed, compatibility square open, all three routes now confirmed to converge on the same missing fact (integral-closure-equals-valuation-ring-extension for a finite extension of a complete field), reached from different Mathlib angles. Full detail in `langlands/ROADMAP.md`, Phase 2a, "Status 2026-08-05 (third pass)".

**Update 2026-08-05 (fourth pass) — Phase 2a primitives (1) and (2) CLOSED; compatibility square still open, now more tractable.** The "missing primitive" three prior sessions searched for — that `w.adicCompletionIntegers L` is the integral closure of `v.adicCompletionIntegers K` in `w.adicCompletion L`, hence `Module.Finite`/`Module.Free` — turned out to **already exist in this repo**, unrecognized: `NormMap.lean`'s `isIntegral_of_mem_of_comap_eq` (landed one session before the third pass, commit `be81faa`) proves exactly the "hard direction" (elements of the top valuation ring are integral over the base, via completeness + Galois conjugates), but had only ever been applied pointwise inside `localNormMap_mem_units`. The "easy direction" (integral elements land in the top valuation ring) was genuinely missing and is new: `Valuation.isIntegral_imp_map_le_one`, a standalone generalization of the forward half of `Valuation.Integers.isIntegral_iff_v_le_one` dropping its bundled surjectivity field. New file `langlands/Langlands/AdicCompletionIntegralClosure.lean` (commit `106870a`, zero `sorry`) combines both into an `IsIntegralClosure` instance, then `Module.Finite`/`Module.Free` via `IsIntegralClosure.finite`/`.module_free`, under an added `Algebra.IsSeparable` hypothesis (automatic in char 0/number fields, matching those Mathlib lemmas' own scope). Primitive (2) — formulating unramifiedness for valuation-ring extensions, previously not statable at all in this repo's general-`e` setting — is also closed: new file `langlands/Langlands/UnramifiedValuationExtension.lean` (commit `61eab9b`, zero `sorry`) defines `IsUnramified` as `𝔪_K·O_L = 𝔪_L` and proves the containment `≤` holds unconditionally, using `Ideal.ramificationIdx'`/`IsLocalRing.map_maximalIdeal_le`, both already general enough in Mathlib to apply directly (no new ramification theory needed). The compatibility square (`localNormMap` reduces mod 𝔪 to the `ResidueFieldNorm.lean` residue norm) was **not attempted this session** per the task's explicit permission not to force it, but is now substantially more tractable: with `Module.Free` in hand, the remaining gap is (a) a basis of a finite free module over a local ring reduces to a basis of the quotient (Nakayama-type, not yet located in Mathlib), (b) under `IsUnramified`, that quotient is the residue field, (c) `Algebra.norm` reduces mod the ideal to the determinant in the reduced basis. Full detail (file:line citations, exact remaining steps) in `langlands/ROADMAP.md`, Phase 2a, "Status 2026-08-05 (fourth pass)". Full `lake build` green (8670 jobs), `grep -rn sorry langlands/Langlands/` empty. **Process note for future sessions**: before concluding a fact is "missing from Mathlib," also check whether it already exists in this repo's own files under a different name — that's exactly what happened here across three sessions.

- **Mathlib upstreaming candidate (new, flagged not attempted):** `Valuation.isIntegral_imp_map_le_one` (`langlands/Langlands/AdicCompletionIntegralClosure.lean:67`) and the broader fact it's part of — a valuation subring extending a base valuation ring (`Valuation.HasExtension`) over a *complete* base field, in a finite extension, equals the integral closure of the base — look like a genuine gap in Mathlib's own `Valuation`/`ValuationSubring` API (Serre, *Local Fields*, Ch. II §2), not specific to this project's `HeightOneSpectrum`/adele setup. No PR attempted.

**Update 2026-08-05 (fifth pass) — Phase 2a compatibility square CLOSED.** New file `langlands/Langlands/NormMapResidueCompatibility.lean`, zero `sorry`, full `lake build` green (8670 jobs). Closed all three remaining steps (a)-(c) the fourth pass left open, plus a fourth piece the fourth pass's sketch had conflated away: (a) `IsLocalRing.basisQuotient` (`Mathlib/RingTheory/LocalRing/Quotient.lean`) — a basis of a finite free module over a local ring reduces to a basis of the quotient — turned out to already exist in Mathlib, found via a fresh loogle query the fourth pass hadn't tried (`Module.Free, IsLocalRing, Module.Basis`); (b)+(c) combine into a fully general, valuation-free lemma `IsLocalRing.residue_norm_eq_norm_residue` (`Algebra.norm` commutes with reduction mod the maximal ideal for any finite free algebra over a local ring), via `RingHom.map_det` + `Algebra.leftMulMatrix_eq_repr_mul` + `basisQuotient`'s companion `basisQuotient_repr`/`basisQuotient_apply`; specializing to `K₀`/`L₀` under `IsUnramified` needed one extra fix not anticipated — `rw [hU]` fails with "motive is not type correct" because `IsLocalRing.basisQuotient`'s ambient `Ideal.Quotient.algebraQuotientMapQuotient` instance is pinned to the exact syntactic ideal form and doesn't generalize, *and* the residue-field algebra structure this repo actually uses (`Langlands.ResidueFieldNorm`, via `Valuation.HasExtension`) is a *different* `Algebra` term from that one even after the ideal is identified — fixed via `Algebra.algebra_ext` (two `Algebra` instances agreeing as terms once their `algebraMap`s agree pointwise, which both do here by `rfl`-level Mathlib lemmas on each side). The fourth piece: connecting the *ring*-norm `Algebra.norm K₀` to the *field*-norm inside `localNormMap` (the fourth pass's sketch didn't separate these), via `Algebra.algebraMap_intNorm` + `Algebra.intNorm_eq_norm` (`Mathlib/RingTheory/IntegralClosure/IntegralRestrict.lean`), needing three more `inferInstanceAs`-pattern instances at the `K₀`/`L₀` level (`IsDomain`, `IsIntegrallyClosed`, `IsFractionRing K₀ K`) plus `Algebra.IsIntegral K₀ L₀` (from `Module.Finite`) and a hand-built `Module.IsTorsionFree K₀ L₀` (mirroring `AdicCompletionIntegralClosure.lean`'s existing pattern). Final theorem `localNormMap_reduce`: under `IsUnramified`, the residue of `localNormMap K L v w a` equals `Algebra.norm 𝓀[K]` of the residue of `a`, for `a` a local unit — the compatibility square from three prior sessions' writeups, now genuinely closed. **Not attempted**: tying `residueField_units_norm_surjective` through this to get `O_L^× ↠ O_K^× / (1+𝔪_K)` — believed to compose cleanly by inspection (compose `ValuationSubring.surjective_unitGroupToResidueFieldUnits` at each of `K₀`/`L₀` with `residueField_units_norm_surjective` in between, then match via `localNormMap_reduce`), scoped as the concrete next step in `langlands/ROADMAP.md`, Phase 2a "fifth pass" entry, but out of scope for this session per the task's brief (compatibility square was the main goal, this composition an optional stretch not to be forced). Full detail in `langlands/ROADMAP.md`, Phase 2a, "Status 2026-08-05 (fifth pass)".

- **Mathlib upstreaming candidate (new, flagged not attempted):** `IsLocalRing.residue_norm_eq_norm_residue` (`langlands/Langlands/NormMapResidueCompatibility.lean:63`) and `residue_norm_eq_norm_residue_of_eq_map` (`:82`) — "`Algebra.norm` commutes with reduction mod the maximal ideal, for a finite free algebra over a local ring" — are stated with no reference to `HeightOneSpectrum`/adic completions at all and look like a natural, self-contained addition to `Mathlib.RingTheory.LocalRing.Quotient` or `Mathlib.RingTheory.Norm.Defs`. No PR attempted.

**Update 2026-08-05 (sixth pass) — Phase 2a mod-principal-units surjectivity CLOSED.** New file `langlands/Langlands/UnitGroupModPrincipalUnitsSurjective.lean` (commit pending in this session), zero `sorry`, `lake build Langlands.UnitGroupModPrincipalUnitsSurjective` and full `lake build` both green (8670 jobs; `grep -rn sorry langlands/Langlands/` empty). Closed exactly the composition the fifth pass scoped as its next step: `localNormMap_units_surjective_mod_principalUnits` — for every `t : (ResidueField K₀)ˣ` (a class of `O_K^× / (1+𝔪_K)`), there is a local unit `a` of `L₀ = w.adicCompletionIntegers L` with `localNormMap K L v w a` reducing mod `𝔪_K` to `t`, i.e. **`N_{L/K} : O_L^× ↠ O_K^× / (1+𝔪_K)`**. The API-mismatch flagged at the end of the fifth pass (`localNormMap_mem_units`'s `Submonoid.units` vs. `ValuationSubring.unitGroupToResidueFieldUnits`'s purpose-built `.unitGroup`) needed no reconciliation lemma — the proof routes around `ValuationSubring.unitGroup` entirely via `Submonoid.unitsEquivUnitsType : S.units ≃* Sˣ` (general submonoid API) plus a fully general fact for any local ring, `IsLocalRing.surjective_units_map_of_local_ringHom` applied to `IsLocalRing.residue_surjective` (reduction mod the maximal ideal is surjective on unit groups), composed with `residueField_units_norm_surjective` (`Langlands.ResidueFieldNorm`) and `localNormMap_reduce` (`Langlands.NormMapResidueCompatibility`, fifth pass). No new instances or general Mathlib-adjacent lemmas were needed — exactly the "session-sized" scope the fifth pass predicted. **Full unramified norm-group theorem (`N_{L/K}(L^×) = ⟨π⟩^n·O_K^×`) not attempted, per the task's brief** — the Hensel-type successive-approximation route from mod-principal-units surjectivity to full `O_K^×` surjectivity is worked out and written up in `langlands/ROADMAP.md`'s new "Status 2026-08-05 (sixth pass)" entry (three steps: graded principal-units filtration matching under `IsUnramified`, a norm-linearizes-to-trace identity on each graded piece — genuinely new content, comparable in size to the fourth/fifth passes' work, not yet in Mathlib or this repo — and a Cauchy-sequence assembly argument using completeness of `adicCompletion`), but not built. This matches the original Phase 2a scoping, which explicitly deferred the principal-units filtration machinery to a later milestone. Full detail in `langlands/ROADMAP.md`, Phase 2a, "Status 2026-08-05 (sixth pass)".

**Update 2026-08-05 (seventh pass) — Phase 2a step 2 (norm linearizes to trace on graded pieces) CLOSED, for every filtration level.** New file `langlands/Langlands/NormTraceLinearization.lean` (commit `6529078`), zero `sorry`, `lake build Langlands.NormTraceLinearization` and full `lake build` both green (8670 jobs; `grep -rn sorry langlands/Langlands/` empty). The classical computation `N(1+πx) ≡ 1+Tr(x)·π mod π²` turned out to be the `k=1` case of a fact Mathlib already has as `Matrix.det_one_add_smul` (`Mathlib.LinearAlgebra.Matrix.Charpoly.Coeff` — the determinant Taylor expansion `det(1+tA) = 1+tr(A)·t+O(t²)`), found by one loogle query, first try: `Algebra.exists_norm_one_add_smul_eq` (`NormTraceLinearization.lean:83`) applies it to the left-multiplication matrix, giving `∃ c, Algebra.norm R (1+t•x) = 1+t·Algebra.trace R S x + t²·c` for any finite free algebra `S/R`, no valuation content. `IsLocalRing.residue_trace_eq_trace_residue`/`_of_eq_map` (`:110`, `:129`) is the trace analogue of the fifth pass's `residue_norm_eq_norm_residue`/`_of_eq_map`, proved the same way (`IsLocalRing.basisQuotient` + entrywise reduction) but easier — trace is a sum of diagonal entries, commuting with any ring hom via `AddMonoidHom.map_trace`, no determinant step needed. Specializing to `K₀`/`L₀` under `IsUnramified` (`residue_trace_eq_trace_residue_of_isUnramified`, `:164`) mirrors the fifth pass's norm specialization exactly. The main theorem, `exists_norm_one_add_uniformizer_pow_smul_eq_trace_add` (`:194`): for `π` a uniformizer of `K₀` and `n : ℕ`, `∃ y, Algebra.norm K₀ (1+π^{n+1}•x) = 1+π^{n+1}·y ∧ residue K₀ y = Algebra.trace 𝓀[K] (residue L₀ x)`. **Closed for every filtration level `n`, not just `i=1` as the task's brief allowed scoping down to** — the same proof with `t := π^{n+1}` works uniformly for all `n ≥ 0`, since it only ever uses `π^{n+1} ∈ 𝔪_K`, so there was no reason to scope down. **Not built, left for step 3's assembly**: bundled quotient-group objects for `(1+𝔪_K^i)/(1+𝔪_K^{i+1})` (the result is stated as a concrete algebraic identity, not a genuine group hom between bundled quotients), and the bridge from this `Algebra.norm K₀`/ring-level identity to `localNormMap`/field level (the analogous bridge, `algebraMap_norm_eq_norm_algebraMap`, already exists for the norm-compatibility square from the fifth pass and should transfer without new difficulty, but wasn't written since not needed to state step 2's content). Step 3 (Cauchy-sequence assembly via completeness of `adicCompletion`) remains untouched, as scoped. With steps 1 and 2 both closed, only step 3 stands between the current state and the full unramified norm-group theorem `N_{L/K}(L^×) = ⟨π⟩^n·O_K^×`. Full detail in `langlands/ROADMAP.md`, Phase 2a, "Status 2026-08-05 (seventh pass)".

**Update 2026-08-05 (eighth pass) — Phase 2a step A gaps closed; step 3's one-step correction and continuity ingredients built, full Cauchy-sequence assembly not attempted.** Three new files, zero `sorry`, full `lake build` green (8670 jobs). Closed `NormTraceLinearization.lean`'s two flagged gaps (commit `796ea51`): `isUnit_one_add_uniformizer_pow_smul` (`1 + π^{n+1}•x` is a unit of `L₀`, no `IsUnramified` needed, only the always-true forward containment `𝔪_K·L₀ ≤ 𝔪_L`) and the bridge to `localNormMap` (`isUnit_algebraMap_one_add_uniformizer_pow_smul`, `localNormMap_coe_one_add_uniformizer_pow_smul`), confirming the seventh pass's prediction that this bridge would "transfer without new difficulty" from the fifth pass's `localNormMap_reduce` pattern. New file `PrincipalUnitsCauchySequence.lean` (commit `a0359bd`): `exists_one_add_uniformizer_pow_smul_norm_sub_mem`, the single Hensel-lift correction step — given a residual error `t : K₀` at filtration level `n`, produces `z : L₀` correcting `N(1+π^{n+1}•z) ≡ 1+π^{n+1}·t (mod π^{n+2})`, via `Algebra.trace_surjective` on the residue fields (finiteness of `𝓀[K]` derived from finiteness of `𝓀[L]` by injectivity of the algebra map, giving the `PerfectField`/`Algebra.IsSeparable` instances `Algebra.trace_surjective` needs) composed with the seventh pass's norm-linearizes-to-trace identity. New file `NormMapContinuity.lean` (commit `72871b5`): `continuous_norm_adicCompletion` — the field-level norm underlying `localNormMap` is continuous. **This directly resolves the task brief's flagged concern that norm-continuity on a finite extension of complete valued fields might be a genuine Mathlib infrastructure gap — it is not**: the proof composes three pre-existing general Mathlib facts (`LinearMap.continuous_of_finiteDimensional`, `Continuous.matrix_det`, `Algebra.norm_eq_matrix_det`) against typeclass instances already sitting in `NormMap.lean` for the unrelated purpose of building `localNormMap` itself — no new instance, no new general lemma, closed in one file on the first attempt. **What remains for the full unramified norm-group theorem is exactly the construction of the successive-approximation sequence itself** — packaging the one-step correction into a recursively-defined `x : ℕ → L₀ˣ` (needs strong recursion with choice, since each step's witness only exists, isn't computed), proving it Cauchy via `Valued.hasBasis_uniformity`/`Filter.HasBasis.cauchySeq_iff`, and closing with `cauchySeq_tendsto_of_complete` plus the continuity lemma above — genuine, bookkeeping-heavy Lean engineering (tracking `π`-adic valuations through a multiplicative recursion) comparable in size to the sixth/seventh passes' files, but with no outstanding "does Mathlib even have this" question left open going in, unlike every prior Phase 2a session. Full detail in `langlands/ROADMAP.md`, Phase 2a, "Status 2026-08-05 (eighth pass)".

**Update 2026-08-05 (ninth pass) — Phase 2a full unramified norm-group surjectivity CLOSED: `N_{L/K}(L^×) ⊇ O_K^×`.** New file `langlands/Langlands/UnramifiedNormSurjective.lean` (commits `a21df52`, `4e1ac36`), zero `sorry`, full `lake build` green (8670 jobs). Same-session continuation of the eighth pass, pushed through to completion at a peer session's request. Main theorem `exists_isUnit_algebraMap_norm_eq_of_isUnramified`: under `IsUnramified K L v w`, every unit `y : K₀` is the exact norm `Algebra.norm K₀ x` of some unit `x : L₀`. Built the base case directly from residue-field norm surjectivity (bypassing `localNormMap`, one fewer bridging layer than the eighth pass expected), restated it and the one-step correction as an **exact multiplicative invariant** `y = N(x)·(1+π^{n+1}·t)` (not the additive congruence sketched earlier — this is what lets `approxUnit (n+1) = approxUnit n·(1+π^{n+1}•z)` hold *definitionally*), built the successive-approximation sequence by structural recursion with the correction witness exposed inside the recursive definition, proved it Cauchy via the `NontriviallyNormedField` structure already sitting in `NormMap.lean` and `cauchySeq_of_le_geometric` (a simpler substitute for the `Valued.hasBasis_uniformity` route the eighth pass anticipated), and closed with `cauchySeq_tendsto_of_complete` + `continuous_norm_adicCompletion` (eighth pass) + `squeeze_zero_norm` for the error term's vanishing. **No genuine Mathlib wall was hit** — every fact the eighth pass flagged as needed either existed as anticipated or was superseded by an equally-available simpler route; the entire pass was proof engineering (a multiplicative Hensel-lift recursion and its limit), not lemma discovery. **Not closed**: the full classical statement `N_{L/K}(L^×) = ⟨π⟩^n·O_K^×` — the reverse inclusion and the `⟨π⟩^n` uniformizer-power factor are untouched (believed comparatively routine but not attempted, the task having scoped to surjectivity). Full detail in `langlands/ROADMAP.md`, Phase 2a, "Status 2026-08-05 (ninth pass)".

**Update 2026-08-05 (tenth pass) — Phase 2a CLOSED in full: `N_{L/K}(L^×) = ⟨π⟩^n · O_K^×`.** New file `langlands/Langlands/UnramifiedNormRange.lean` (commit `d5ec480`), zero `sorry`, full `lake build` green (8670 jobs; `grep -rn sorry langlands/Langlands/` empty). Re-derived the reverse inclusion `N_{L/K}(L^×) ⊆ ⟨π⟩^n·O_K^×` from the actual math rather than trusting the ninth pass's "believed routine" hedge, and it held up exactly as sketched: every unit of `w.adicCompletion L` decomposes as `(uniformizer)^k · (L₀-unit)`, `k : ℤ`, via `IsDiscreteValuationRing.associated_pow_irreducible` (confirmed present in Mathlib) once `IsUnramified` is used to show a `K₀`-uniformizer stays irreducible in `L₀`; `localNormMap` of the uniformizer is exactly `π^n` via `Algebra.norm_algebraMap` applied directly at the field level (`n := Module.finrank (v.adicCompletion K) (w.adicCompletion L)`, the local degree — working at field level from the start sidesteps the "which finrank" question the task flagged as open, since the ring-level `finrank K₀ L₀` never needs to appear or be compared against it). `IsDiscreteValuationRing (v/w.adicCompletionIntegers _)` turned out to be unconditional Mathlib instances (`Mathlib.NumberTheory.NumberField.Completion.FinitePlace`), not gated on any number-field hypothesis — resolving the task's "may itself be a real sub-development" concern in the negative. Main theorem `localNormMap_range_eq`: `MonoidHom.range (localNormMap K L v w) = Subgroup.zpowers (uniformizerUnit ^ n) ⊔ (v.adicCompletionIntegers K).units`, an equality of subgroups of `(v.adicCompletion K)ˣ` using existing Mathlib vocabulary throughout (`Subgroup.zpowers`, `Submonoid.units`, `Subgroup.mem_sup` for the pointwise-product characterization of a sup in an abelian group) — no hand-rolled statement shape was needed. **No genuine Mathlib wall was hit anywhere in this pass**; the only friction was ordinary `algebraMap`-vs-plain-coercion syntactic mismatches in a few `rw`/`rfl` steps, resolved by matching notation style to each call site. **Phase 2a is now fully closed**: this is the first full classical theorem-statement (not a one-directional inclusion or supporting lemma) this project has closed end-to-end from an `IsUnramified` hypothesis with zero `sorry`. Phase 2 (full local reciprocity, the ramified/Lubin–Tate case) remains untouched — see `langlands/ROADMAP.md`'s Phase 2a "tenth pass" status entry for a precise statement of what Phase 2 needs next given everything now available.

### [x] Update CLAUDE.md — corrections as documentation lag (2026-03-29)

Add to the corrections section:
> **Corrections are documentation lag, not model failure.** When the same mistake recurs, the fix is writing the invariant down — not repeating the correction. Every correction that doesn't produce a CLAUDE.md edit will happen again. Exception: during active design, corrections are the work itself — don't prematurely document a design that hasn't settled yet.

Add to the Session Handoff section:
> **Initiate a handoff after a significant mid-session correction.** When a correction happens after substantial wrong-path work, the wrong reasoning is still in context and keeps pulling. Writing down the invariant and starting fresh beats continuing with poisoned context — the next session loads the invariant from turn 1 before any wrong reasoning exists.

Conventional commit: `docs: add corrections-as-documentation-lag + context-poisoning handoff rule`
